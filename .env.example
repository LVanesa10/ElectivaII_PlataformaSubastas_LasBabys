# Puerto en el que escucha el servidor HTTP
PUERTO=3000

# Secreto usado para firmar y verificar los tokens de sesion (JWT).
# Genere un valor propio y unico para cada entorno; nunca use este valor en produccion.
JWT_SECRETO=
